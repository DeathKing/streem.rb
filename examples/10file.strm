class Student
  def initialize(name, age)
  	@age = age
  	@name = name
  end
end

studentlize = lambda do |str|
	Student.new(*str.split(","))
end

file("student.csv").
  | line_io.
  | chomp.
  | studentlize.
  | STDOUT